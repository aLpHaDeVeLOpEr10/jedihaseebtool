// ─────────────────────────────────────────────
//  Central config — change APP_NAME here and it
//  propagates to title, heading, and metadata.
// ─────────────────────────────────────────────

export const APP_NAME = "Image Upscaler Tool";
export const LOGO_TEXT = "ImageUp";
export const DEFAULT_SCALE_FACTOR = 2;
export const API_BASE_URL = "http://localhost:8000";
export const MAX_FILE_SIZE_MB = 30;
export const SUPPORTED_FORMATS = ["PNG", "JPG", "JPEG", "WEBP"];
